<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bookmanager.mapper.IndexMapper">
    <select id="getAllBooks" resultType="Book">
        select * from book;
    </select>

    <select id="getAllBooksOrderByHot" resultType="Book">
        select * from book order by hot desc limit 0,5;
    </select>

    <select id="getBookByName" resultType="Book" parameterType="string">
        select * from book where name like '%${name}%' order by hot desc;
    </select>

    <select id="geAllType" resultType="string" parameterType="Book">
        select type from (
            select type, sum(hot) as total from book group by type order by total desc) as t;
    </select>

    <select id="getBooksByType" resultType="Book">
        select * from book where type = #{type} order by hot desc;
    </select>
</mapper>