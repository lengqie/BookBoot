<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bookmanager.mapper.AdminMapper">
    <select id="login" resultType="cn.bookmanager.entity.Admin">
        select * from admin where name=#{name} and password =#{password}
    </select>
    <select id="isLogin" resultType="int" parameterType="Admin">
        select count(1) from admin where name=#{name} and password =#{password}
    </select>

    <select id="getAdminByName" resultType="Admin" parameterType="string">
        select * from admin where name =#{name}
    </select>

    <update id="updateUser" parameterType="User">
        update user set name = #{name}, password = #{password}, defaultCount =#{defaultCount}, balance =#{balance}
        where id = #{id};
    </update>
    <update id="updateBook" parameterType="Books">
        update books set Isbn =#{isbn},name =#{name}, type = #{type}, num = #{num} where isbn = #{isbn};
    </update>

    <update id="updateRecord">
        update record set success = #{success} where record_id = #{recordId};
    </update>

</mapper>