<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bookmanager.mapper.BooksMapper">

    <select id="getBooksInfo" parameterType="string" resultType="Books">
        select * from books where Isbn = #{Isbn}
    </select>

    <update id="addHot" parameterType="string">
        update books set hot =hot +1 where Isbn =#{Isbn};
    </update>

    <select id="getBookByIsbn" parameterType="string" resultType="Books">
        select * from books where Isbn =#{isbn}
    </select>

    <update id="borrowBooks">
        update books set num = num - 1 where Isbn = #{isbn};
        update user set count = count + 1 where id = #{userId};
        update user set total = total + 1 where id = #{userId};
        insert into record values (#{recordId},#{isbn},#{userId},#{time},#{days},0);
    </update>

    <update id="returnBooks">
        update books set num = num + 1 where Isbn = #{isbn};
        update user set count = count - 1 where id = #{userId};
        update record set success = 1 where record_id = #{recordId}
    </update>

</mapper>